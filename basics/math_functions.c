#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main(void)
{

    float x = 45;

    // x = sqrt(x);
    // x= pow(x,2);
    // x= round(x);
    // x = floor(x);
    // x = abs(x);
    // x = log(x);
    // x = sin(x);
    // x = cos(x);
    // x = tan(x);

    printf("%.2f\n", x);

    return 0;
}