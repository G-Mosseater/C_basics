#include <stdio.h>
#include <stdbool.h>

int main(void)
{

    bool isSunny = true;

    if (!isSunny)
    {
        printf("It is NOT sunny outside \n");
    }
    else
    {
        printf("It is  sunny outside \n");
    }
    // int temp = -1;

    // if (temp > 0 && temp < 30)
    // {
    //     printf("The temperature is good xD\n");
    // }
    // else
    // {
    //     printf("The temperature is not good man\n");
    // }

    return 0;
}